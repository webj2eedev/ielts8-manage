<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webj2eedev.ieltsnote.dao.WordLogDAO">
    <insert id="log">
        insert into ieltsnote.word_log (creator, count, log_time)
        select creator, count(w.word) as `count`, NOW() as log_time from ieltsnote.word w group by creator
    </insert>
</mapper>

